spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/primes
    username: postgres
    password: Mercedes

  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  sql:
    init:
      mode: always

server:
  port:
    8085